services:
  # ─── Backend: Bloodchain Core ───────────────────────
  - type: web
    name: bloodchain-core
    env: node
    plan: free
    rootDir: bloodchain-core
    installCommand: npm install --legacy-peer-deps
    buildCommand: npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: bloodchain-db
          property: connectionString
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_ANON_KEY
        sync: false
      - key: SUPABASE_JWT_SECRET
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false

  # ─── Frontend: Azure (Mobile) ───────────────────────
  - type: web
    name: bloodchain-azure
    env: static
    rootDir: azure
    installCommand: npm install --legacy-peer-deps
    buildCommand: npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        # Using interpolation for absolute URL
        value: https://${bloodchain-core_host}/api/v1
      - key: VITE_SUPABASE_URL
        sync: false
      - key: VITE_SUPABASE_ANON_KEY
        sync: false

  # ─── Frontend: Azure-Clone (Desktop) ────────────────
  - type: web
    name: bloodchain-azure-clone
    env: static
    rootDir: azure-clone
    installCommand: npm install --legacy-peer-deps
    buildCommand: npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://${bloodchain-core_host}/api/v1
      - key: VITE_SUPABASE_URL
        sync: false
      - key: VITE_SUPABASE_ANON_KEY
        sync: false

  # ─── Frontend: High Command (Admin) ─────────────────
  - type: web
    name: bloodchain-high-command
    env: static
    rootDir: high-command
    installCommand: npm install --legacy-peer-deps
    buildCommand: npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://${bloodchain-core_host}/api/v1
      - key: VITE_SUPABASE_URL
        sync: false
      - key: VITE_SUPABASE_ANON_KEY
        sync: false

  # ─── Frontend: Mars Lab ─────────────────────────────
  - type: web
    name: bloodchain-mars-lab
    env: static
    rootDir: mars-lab
    installCommand: npm install --legacy-peer-deps
    buildCommand: npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://${bloodchain-core_host}/api/v1
      - key: VITE_SUPABASE_URL
        sync: false
      - key: VITE_SUPABASE_ANON_KEY
        sync: false

  # ─── Frontend: Scyther ──────────────────────────────
  - type: web
    name: bloodchain-scyther
    env: static
    rootDir: scyther
    installCommand: npm install --legacy-peer-deps
    buildCommand: npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://${bloodchain-core_host}/api/v1
      - key: VITE_SUPABASE_URL
        sync: false
      - key: VITE_SUPABASE_ANON_KEY
        sync: false

  # ─── Frontend: Voyager ──────────────────────────────
  - type: web
    name: bloodchain-voyager
    env: static
    rootDir: voyager
    installCommand: npm install --legacy-peer-deps
    buildCommand: npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://${bloodchain-core_host}/api/v1
      - key: VITE_SUPABASE_URL
        sync: false
      - key: VITE_SUPABASE_ANON_KEY
        sync: false

  # ─── Demo: Constellation Hub ───────────────────────
  - type: web
    name: bloodchain-demo-hub
    env: static
    rootDir: demo-hub
    installCommand: npm install --legacy-peer-deps
    buildCommand: npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_HIGH_COMMAND_URL
        value: https://${bloodchain-high-command_host}
      - key: VITE_MARS_LAB_URL
        value: https://${bloodchain-mars-lab_host}
      - key: VITE_VOYAGER_URL
        value: https://${bloodchain-voyager_host}
      - key: VITE_SCYTHER_URL
        value: https://${bloodchain-scyther_host}
      - key: VITE_AZURE_URL
        value: https://${bloodchain-azure_host}

databases:
  - name: bloodchain-db
    plan: free
    databaseName: bloodchain
    user: bloodchain_user
